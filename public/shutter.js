// Get elements
const shutterWindow = document.getElementById('shutter-window');

// Open the shutter window
shutterWindow.addEventListener('click', () => {
    shutterWindow.classList.add('open');
});
